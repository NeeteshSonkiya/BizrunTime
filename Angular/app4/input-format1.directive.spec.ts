import { InputFormat1Directive } from './input-format1.directive';

describe('InputFormat1Directive', () => {
  it('should create an instance', () => {
    const directive = new InputFormat1Directive();
    expect(directive).toBeTruthy();
  });
});
